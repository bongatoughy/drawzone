export const ROUTES = {
  REGISTER: `/register`,
  LOGIN: `/login`,
  REFRESH_TOKEN: `/refresh_token`,
  // REGISTER: `${process.env.HOST}/register`,
  // LOGIN: `${process.env.HOST}/login`,
  // REFRESH_TOKEN: `${process.env.HOST}/refresh_token`,
};
